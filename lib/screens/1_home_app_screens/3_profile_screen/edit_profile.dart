import 'package:flutter/material.dart';

class EdirProfileScreen extends StatefulWidget {
  EdirProfileScreen({Key? key}) : super(key: key);

  @override
  _EdirProfileScreenState createState() => _EdirProfileScreenState();
}

class _EdirProfileScreenState extends State<EdirProfileScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("ddd"),
    );
  }
}
